syntax = "proto3";

package api;

service Squarer {
    rpc Square(SquareRequest) returns (SquareResponse){}
}

message SquareRequest {
    int32 arg = 1;
}

message SquareResponse {
    int32 answer = 1;
}

protoc -I api/proto --go_out=plugins=gprc:pkg/api api/proto/squarer.proto